{
  "name": "link-analytics",
  "version": "1.0.0",
  "description": "Web app to generate trackable links and analyze visitor metadata (location, referrer, device, browser)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "useragent": "^2.3.0"
  }
}
